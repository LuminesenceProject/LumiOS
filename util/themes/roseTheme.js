import { createTheme } from "./themeUtil";

const roseTheme = createTheme({
  primary: "#fc7fb6",
  primaryLight: "#ffbbe1",
  secondary: "#dd356e",
  secondaryLight: "#ffbbe1",
  textBase: "white",
});

export default roseTheme;